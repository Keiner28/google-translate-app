{
  "version": 2,
  "builds": [{ "src": "dist/index.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"
    }
  ],
  "installCommand": "npm install --prefix=../..",
  "buildCommand": "cd ../.. && turbo run build --filter='/apps/frontend'"
}
